import { ReactNode } from "react";

export default function SignUp({ children }: { children: ReactNode }) {
    return (
        <section>
            SignUp
            {children}
        </section>
    );
}
